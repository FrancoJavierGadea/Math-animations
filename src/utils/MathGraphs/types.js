

/**
 * @typedef {[min:number, max:number] | {min:number, max:number}} Range
 * 
 * @typedef {[x:number, y:number] | {x:number, y:number}} Point
 * 
 * @typedef {[x:number, y:number, z:number] | {x:number, y:number, z:number}} Point3D
 */

export default {};